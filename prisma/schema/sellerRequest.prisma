enum SRequest {
    PENDING
    APPROVED
    REJECT
}

model SellerRequest {
    id        String   @id @default(uuid())
    userId    String
    address   String?
    status    SRequest @default(PENDING)
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
    user      User     @relation(fields: [userId], references: [id])
}
